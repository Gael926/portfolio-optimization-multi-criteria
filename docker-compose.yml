services:
  finance-app:
    build: .
    image: finance-app:latest
    ports:
      - "${HOST_PORT:-8501}:8501"
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_BROWSER_SERVER_PORT=${HOST_PORT:-8501}
    restart: unless-stopped
